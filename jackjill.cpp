#include <bits/stdc++.h>
using namespace std;

// int equal(int temp[], int arr[], int n)
// {
//     int flag = 0;
//     for (int i = 0; i < n; i++)
//         temp[i] == arr[i] ? flag = 1 : flag = 0;
//     return flag;
// }
int main()
{
    int count = 0;
    int arr[] = {2, 3};
    vector<int> v;
    int n = sizeof(arr) / sizeof(arr[0]);
    for (int i = 0; i < n; i++)
    // int temp[sizeof(arr) / sizeof(arr[0])];
    // memset(temp, 0, sizeof(arr));
    // int n = sizeof(temp) / sizeof(temp[0]);
    // while (equal(temp, arr, n))
    // {
    //     for (int i = 0; i < n; i++)
    //     {
    //         temp[] temp[i++]
    //     }
    // }
    return 0;
}
